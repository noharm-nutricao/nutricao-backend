FROM public.ecr.aws/lambda/python:3.11

RUN yum install -y \
    gcc \
    gcc-c++ \
    make \
    postgresql15-devel

WORKDIR /var/task

COPY requirements-nitra.txt .

RUN pip install --no-cache-dir -r requirements-nitra.txt

COPY . .

CMD ["app.lambda_handler.handler"]