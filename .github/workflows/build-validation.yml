name: build-validation.yml

on:
  push:
    branches: [develop]
  pull_request:
    branches: [develop]

permissions:
  contents: read
  pull-requests: write

jobs:
  docker-build-and-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo principal
        uses: actions/checkout@v4

      - name: Clonar repo do database
        run: |
          git clone https://github.com/noharm-nutricao/nutricao-database.git database

      - name: Build containers
        run: docker compose -f docker-compose.ci.yml build

      - name: Subir stack
        run: docker compose -f docker-compose.ci.yml up -d

      - name: Mostrar containers
        run: docker compose -f docker-compose.ci.yml ps

      - name: Aguardar API
        run: |
          for i in {1..40}; do
            if curl -fsS http://localhost:5000/nutritional/healthz; then
              echo "API respondeu com sucesso"
              exit 0
            fi

            echo "Aguardando API subir... tentativa $i"
            docker compose -f docker-compose.ci.yml ps
            sleep 5
          done

          echo "API não respondeu. Exibindo logs:"
          docker compose -f docker-compose.ci.yml logs
          exit 1

      - name: Derrubar
        if: always()
        run: docker compose -f docker-compose.ci.yml down -v

      - name: Logs em caso de falha
        if: failure()
        run: docker compose -f docker-compose.ci.yml logs

  sonar:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_USER: postgres
          POSTGRES_DB: noharm
          POSTGRES_HOST_AUTH_METHOD: trust
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Instalar dependências
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Setup PostgreSQL
        run: |
          git clone --branch develop https://github.com/noharm-nutricao/nutricao-database
          psql -h localhost -U postgres -d noharm -f nutricao-database/migrations/flyway/V1__create_public_schema.sql -v ON_ERROR_STOP=1
          psql -h localhost -U postgres -d noharm -f nutricao-database/migrations/flyway/V2__create_demo_schema.sql -v ON_ERROR_STOP=1
          psql -h localhost -U postgres -d noharm -f nutricao-database/migrations/flyway/V3__create_triggers.sql -v ON_ERROR_STOP=1
          psql -h localhost -U postgres -d noharm -f nutricao-database/migrations/flyway/V4__seed_data.sql -v ON_ERROR_STOP=1
          psql -h localhost -U postgres -d noharm -f nutricao-database/migrations/flyway/V5__nitra_test_seed.sql -v ON_ERROR_STOP=1

      - name: Rodar testes com cobertura
        env:
          ENV: test
          DB_HOST: localhost
          DB_PORT: "5432"
          DB_NAME: noharm
          DB_USER: postgres
          DB_PASSWORD: ""
        run: |
          python -m pytest tests/unit/test_nutritional*.py tests/integration/test_nutritional*.py --cov=services/nutritional --cov=routes/nutritional --cov-report=xml:coverage.xml

      - name: SonarCloud Scan
        uses: SonarSource/sonarcloud-github-action@master
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}

      - name: Comentar resultado no PR
        if: github.event_name == 'pull_request'
        uses: actions/github-script@60a0d83039c74a4aee543508d2ffcb1c3799cdea  # v7.0.1
        with:
          script: |
            const fs = require('fs');
            const reportPath = '.scannerwork/report-task.txt';
            if (fs.existsSync(reportPath)) {
              const content = fs.readFileSync(reportPath, 'utf8');
              const match = content.match(/projectKey=(.*?)(?:\n|$)/);
              const projectKey = match ? match[1] : 'unknown';
              github.rest.issues.createComment({
                issue_number: context.issue.number,
                owner: context.repo.owner,
                repo: context.repo.repo,
                body: `## SonarCloud Quality Gate Report\n\n✅ Analysis completed. Check [SonarCloud Dashboard](https://sonarcloud.io/project/overview?id=${projectKey}) for details.\n\nQuality Gate must pass for merge:\n- New Code Coverage: >= 80%\n- New Code Duplication: <= 3%\n- Maintainability Rating: A\n- Reliability Rating: A\n- Security Rating: A\n- Security Hotspots Reviewed: 100%`
              });
            }
